server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/url_shortener
    username: user1
    password: admin

  jpa:
    hibernate:
      ddl-auto: update   # dev only (later: validate)
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  jackson:
    serialization:
      indent-output: true